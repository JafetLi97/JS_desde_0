// sintaxis del op ternario:

// condicion ? expresion1 : expresion2

// Expresión1  si evalúa a true Y expresión2 si evalúa a false

 // ejemplo
 let edad = 18;

 let accseso = edad > 17 ? "Acceso permitido" : "Acceso denegado";

 console.log(accseso);

 

